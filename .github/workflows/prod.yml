name: Prod Build

on:
  push:
    branches:
      - main

jobs:
  build-prod:
    runs-on: self-hosted
    defaults:
      run:
        working-directory: /home/petya/projects/yt-music
    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Run make prod
        run: make prod